<% unless @photos.empty?  %>
<%#= gmaps(
			
			"markers" => { "data" => @json },
			"map_options" => {"auto_adjust" => "true", "do_clustering" => "true" }
			
	) %>
	<%= gmaps(:markers => {:data => @json, :options => { :do_clustering => true}	}) %>
<% else %>
	<%= gmaps(
		:markers => {:data => @json, :options => { :do_clustering => true}}
		) %>
 %> 
<% end  %>	

  <%#= gmaps4rails(@json) %>
  <%#= gmaps("markers" => {"data" => @json},
     "map_options" => { 
     		"auto_adjust"=>true, "do_clustering"=>true 
     },
     :last_map    => false

     ) %> 

