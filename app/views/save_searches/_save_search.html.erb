<%= form_for save_search do |f| %>
  
  
  <%= link_to save_search.name, root_path %>
  
  <%= link_to "(remove)", save_search, method: :delete, data: {confirm: "Are you sure?"}, remote: true %>
<% end %>
