<div class="owner-popup">
  <div class="page-header">
  	<button type="button" class="close">×</button>
  	<h1>New Brand Owner</h1>
  </div>

<%= simple_form_for @brand_owner, remote: true, :html => { :class => 'form-horizontal' } do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name %>
    <%= f.input :description %>
  </div>

  <div class="form-actions">
    <%= f.button :submit, :class => 'btn btn-primary', remote: true  %>
      <%= link_to 'Cancel', '#', :class => 'btn cancel' %>
  </div>
<% end %>

</div>	
