  <div class="page-header">
    <h1><%= @store.name %></h1>
    
  </div>

<div class="">
  <p id="notice"><%= notice %></p>
  <div class="action pull-right">
    <%= link_to new_audit_path(:retailer => @store.retailer.name, :store => @store.name),
        :class => 'btn btn-primary' do %>
        <i class="icon-upload icon-white"></i>
        <span>Upload Images</span>
    <% end %>    
    <%= link_to edit_store_path(@store), :class => 'btn btn-primary' do %>
        <i class="icon-edit icon-white"></i>
        <span>Edit</span>
    <% end %>    
  </div>
  
  <p>
    <% unless @store.address.empty? %>
      <%= @store.address %>,
    <% end %>
    <% unless @store.address.empty? %>
      <%= @store.address2 %>,
    <% end %>
    <% unless @store.address3.empty? %>
      <%= @store.address3 %><br />
    <% end %>
    <% unless @store.town.empty? %>
      <%= @store.town %>
    <% end %>
    <% unless @store.postcode.empty? %>
      <%= @store.postcode %>,
    <% end %>
    <%= @store.country.name unless @store.country_id.nil? %>
  </p>
  <p>
    <strong>Sector: </strong>
    <%= @store.retailer.sector.name unless @store.retailer.sector_id.nil? %>
  </p>
  <p>
    <strong>Store format:</strong>
    <%= @store.store_format.name %>
  </p>
  <p>
    <% if @store.channel_id.present? %>
      <strong>Environment Type:</strong>
      <%= @store.environment_type.name %>
    <% end %>
  </p>
  <p>
    <% if @store.channel_id.present? %>
      <strong>Channel:</strong>
      <%= @store.channel.name %>
    <% end %>
  </p>      

    <strong>Retailer:</strong>
    <%= @store.retailer.name %>
  </p>
  <!-- 
  <% unless @audits.nil? %>
    <% unless @audit.nil?  %>
    <p>
      <%#= select_tag "audit_id", options_from_collection_for_select(@audits, "id", "created_at"), :id => 'store-audit-select' %>
    </p>
    
    <section class="store_audit_categories">
      <%#= render 'stores/shared/store_view_categories', :locals => {:audits => @audits}%>
    </section> 
    <% end %>

  <% end %> -->
</div>  